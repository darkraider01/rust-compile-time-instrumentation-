[package]
name = "cargo-instrument-rust-driver"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
instrument-semantics = { path = "../../instrument-semantics" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
same-file = "1.0"

[workspace]
